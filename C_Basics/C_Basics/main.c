#include <stdio.h>

int main() {
	puts("Hello\nMy name is \"Blorf\".");
	/*
	puts("Output values from 1 to 10\n");
	// Output values from 1 to 10
	for (int i = 1; i < 11; i++) {
		printf("%i\n", i);
	}
	puts("Output even values 2 through 20\n");
	// Output even values 2 through 20
	for (int i = 0; i < 21; i++) {
		if (i / 2) {
			printf("%i\n", i);
		}
	}
	puts("Output values 10 through 1\n");
	// Output values 10 through 1
	for (int i = 10; i > 0; i--) {
		printf("%i\n", i);
	}

	
	
	table();
	evaluate(100);
	printMe();
	*/
	//instructions();

	//rightJustify("Haha", 20);

	// pointerString();

	allocate_memory();
	return 0;
}